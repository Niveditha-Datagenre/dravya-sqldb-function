[project]
name = "dravya-sqldb-function"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "azure-functions",
    "requests",
    "pandas",
    "azure-storage-blob"
]
